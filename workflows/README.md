# workflows 目录说明

这里只放 ComfyUI 的 `.json` 工作流文件。

可以提交：

- ComfyUI 导出的工作流 JSON
- 用于说明工作流用途的 Markdown 文档

不要提交：

- `.safetensors`、`.ckpt`、`.pt`、`.pth`、`.bin` 模型文件
- 输入视频
- 生成图片
- 私人参考图
- ComfyUI 缓存文件

推荐命名：

```text
faceid_test.json
character_consistency_v1.json
storyboard_panel_test.json
```
